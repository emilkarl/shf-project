%header.entry-header
  %h1.entry-title= t('.title')

  -# TODO need to already have the checklist defined? order # .... ?

.entry-content
  = render 'form'
  %hr
  .text-center
    = link_to 'All checklist items', checklist_items_path, class: 'btn btn-sm btn-light'
    -#= link_to t('business_categories.all_business_categories'), checklist_items_path, class: 'btn btn-sm btn-light'
